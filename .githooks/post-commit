#!/bin/sh
# post-commit hook.
#
# The previous version warned when the legacy desktop exe (app/resources/app.asar)
# was stale relative to src/. That shell has been archived to legacy/ and the
# referenced check-exe-stale.ps1 no longer exists, so the check was removed
# (production audit P1-B2: dead hook pointing at a nonexistent script).
#
# This hook never blocks the commit (always exits 0). A new-architecture gate
# (e.g. smoke-test -SkipRuntime) can be wired here when CI is introduced.
exit 0
