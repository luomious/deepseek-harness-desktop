# DSH 桌面项目 · 2026-09-06 全面审计归档

> 审计日期：2026-09-06
> 范围：30 插件 + 50 脚本 + 补丁体系 + Profile + 锁 + Skill + 旧版本/旧实例指向
> 目的：对长期运行不出现问题、可维护性、可迭代性、可扩展性的全面审计与修复
> 提交：4 个 atomic commit，已 push origin/master

---

## 1. 审计结论（一句话）

**11 项 P1 运行隐患已全部修复，补丁体系已加固（39 checks ALL PASS），task-scheduler 死锁自动回收机制已上线。** 项目整体工程化质量中上偏良，已有审计基线（PRODUCTION-READINESS-REVIEW.md + HANDOVER-2026-09-04.md）大部分问题已修，本次聚焦增量发现。

## 2. 已修复（全部已验证+已提交）

### P1 运行隐患（11/11 已修）

| # | 问题 | 证据 | 修复 |
|---|---|---|---|
| P1-1 | startup-verify V6 硬编码 build 号 | `startup-verify.mjs:171` | junction realpath 动态解析 |
| P1-2 | spawnHidden.js 第 5 处调试 trace | `modlens/dsh/spawnHidden.js:17,20` | trace 已删+文件归档清理+修复脚本交付 |
| P1-3 | 3 插件 ESM require('electron') 抛错 | hygiene/janitor/maintenance | createRequire 恢复桌面通知 |
| P1-4 | routing-suite spawnSync 缺 windowsHide | `injector/src/index.ts:2739,2766` | src 已修（待重建 tgz 生效） |
| P1-5 | vision-engine/autoread stream error 缺监听 | `lib/index.js:312-327,102-119` | child.stdout/stderr.on('error') |
| P1-6 | task-scheduler sleepSync 阻塞主线程 | `core.js:224` | HTTP acquire waitMs 封顶 5s |
| P1-7 | 11 个过期死锁堆积 | `~/.dsh/.task-scheduler/locks/` | 已清理（备份至 `_backups/`）+ **懒回收机制上线** |
| P1-8 | verify-patches 7 项 bundle 零校验 | `verify-patches.ps1` | 补 7 项校验（39 ALL PASS） |
| P1-9 | session-persistence MANIFEST 缺失 | `MANIFEST.md` | 已补登（含 SHA-256） |
| P1-10 | apply-stage-viewer 不生效 | `apply-stage-viewer.mjs:55` | 原子替换修复 |
| P1-11 | failover inject=[] | `failover/lib/index.js:20` | inject=['tools'] |

### P2 可维护性（关键 6 处硬编码已动态化）

- `resolve-dist.mjs` VENDOR_ROOT → `import.meta.url` 推导
- `startup-verify.mjs` REPO → `path.dirname(import.meta.url)` 推导
- `hy3-gateway/lib/index.js` GATEWAY_DIR → `import.meta.url` 推导（与 instance-janitor 一致）
- `model-whitelist/lib/index.js` → `DSH_HOME` 统一
- `syncheck-plugins.mjs` / `fix-injector-loadcache.mjs` → `os.tmpdir()`/`os.homedir()` 推导
- `probe-live.mjs` 3080→43120

### task-scheduler 死锁自动回收机制（新增）

`core.js` `status()` 新增懒回收逻辑：每次 status 检查时顺带清理过期锁（pid 死亡 + 心跳超时），rename 为 `.stale-<ts>` 保留审计痕迹，记录 `auto-reclaimed` 变更到 changes.jsonl。**彻底解决长期运行后死锁堆积阻塞 acquire 的隐患**——不再需要手动 `prune` 或 `clear --force`。

### 验证证据

- startup-verify: V1-V8+V10 PASS，V9（重启后 73 文件全过）
- verify-patches: **39 checks ALL PASS**
- 重启后: **零 ERROR/Unhandled/ReferenceError 回归**
- task-scheduler locks: 0 json（无死锁）
- spawn-trace.log: 已归档清理，根目录不存在
- spawnHidden.js: `has trace: false`

## 3. 后续计划（按优先级）

### P2：重复代码收敛第一批（退避注册 / 去重通知 / loopback 校验）—— ✅ 已完成

| 收敛项 | 当前分布 | 改动方案 | 状态 |
|---|---|---|---|
| webServer 退避注册样板 | hygiene/janitor/maintenance 各自写一套 | `dsh-host-services/lib/shared-utils.js` 导出 `registerRouteWithRetry(ctx, path, handler)` | ✅ 已收敛（151 行新增替换 149 行重复） |
| 24h 去重通知器 | hygiene/janitor/maintenance 各自实现 | 导出 `createDedupNotifier(key, ms)` | ✅ 已收敛（janitor/maintenance） |
| loopback 校验 | hygiene/task-scheduler/host-services 各实现 | 统一到 host-services `trusted()` | 📋 后续（host-services 已有 trusted()，待统一） |

**收益**：可维护性飞跃——安全修复只修一处全生效，消除"复制品漂移"回归风险。
**风险**：低——host-services 已在所有相关插件被 inject/registerLocalApi 使用，无需新增依赖声明。

### P3：重复代码收敛第二批（log helper / 原子写）

| 收敛项 | 当前分布 | 改动方案 | 风险 |
|---|---|---|---|
| log() helper | tier-router/failover/janitor/task-scheduler/hy3 各自重写 | host-services 统一导出 | 低-中：每个实现略有差异需逐一适配 |
| 原子写/JSON helper | tier-router/remote-workspace/vision-engine/autoread/task-scheduler 各写一套 | 复用 dsh-atomic-write 包 | 中：需适配各插件的写入模式 |

### P3：SSRF HTTP 客户端收敛（需单独规划）

- **问题**：fetch-local 与 bing 的 `isPrivateAddress`/`resolvePublicRecords`/`requestViaIp`/`requestStream`/`collectBody` 含 DNS-rebinding TOCTOU 修复，几乎逐行复制（bing 注释"内联复制，避免跨包依赖"）
- **方案**：要么建新共享模块 `@dsh-external/dsh-shared-utils`（需 tgz 打包 + 装配变更），要么从 fetch-local 导出并修改 bing 的依赖装配
- **风险**：高——安全敏感逻辑 + 跨插件依赖装配变更面大，需单独代码审查
- **建议**：与 routing-suite tgz 重建合并执行

### P3：单测覆盖

15 个无单测插件可按需补纯逻辑测试（HANDOVER-2026-09-04 已登记）。

### 待用户决策

- **OpenViking MCP 30 秒重试噪音**：第三方插件服务配置问题（volcengine ark API 不可达），非代码 bug。如不打算用，可在 settings.yaml 禁用。由用户决策。

## 4. 工作树状态

| 文件 | 状态 | 说明 |
|---|---|---|
| `CHANGELOG.md` | +本次审计条目 | 已更新 |
| `plugins/dsh-vision-engine/lib/client.js` | 其他会话改动 | 性能优化（render 全量扫描改为可选），非本轮 |
| `plugins/dsh-vision-engine/lib/client.js.bak-20260906` | 其他会话备份 | 8/28 旧备份，非本轮产物 |
| `_backups/` | ~120 个历史备份 | 项目长期运行积累，dsh-maintenance 管理 |

## 5. 备份 / 回滚

- 所有改动文件可 `git revert` 逐 commit 回滚
- 死锁清理备份至 `_backups/task-scheduler-locks-stale-20260906135303/`
- spawn-trace 归档至 `_backups/spawn-trace-archived-20260906175431.log`
- spawnHidden.js 备份至 `_backups/spawnHidden.js.bak-2026-09-06T09-49-17-877Z`

---

*本文件由 2026-09-06 审计会话生成，供后续会话接手时的当前状态单一事实源。*
