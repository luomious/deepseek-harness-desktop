# DSH 合并升级 · 执行记录

> 项目:D:\Deepseek-Harness(唯一仓库)+ 官方新壳 dsh-plugin-desktop v2.0.2(唯一运行时)
> 目标:修复全部崩溃点 → 克隆官方源码入库 → 自建带全部插件的桌面应用 → 收敛单实例 → 全程可回滚、可监控
> 开始日期:2026-08-22

---

## 0. 决策与已确认事实

| 事项 | 结论 |
|---|---|
| 保留哪个 | **D 盘仓库为唯一项目**;引擎用官方新壳(先官方 exe,后自建 exe) |
| 合并方向 | 插件生态合并进官方壳的 `desktop` profile;源码克隆进 `vendor/` 做升级 |
| 浏览器弹出问题 | ✅ **已解决**:旧壳 `src/lib/window-ui.js:111/184` 有 `shell.openExternal`;新壳 `cordis.patch.yml` 为 `openBrowser: false, printUrl: false`,UI 内嵌窗口,不再弹浏览器 |
| E 盘删除时机 | 自建 exe 通过 72h 稳定观察并成为唯一实例后,可删除 `E:\DSH\DSH Desktop` 与 `E:\DSH\Desktop-src`;`~/.dsh`(会话/配置)与 `D:\Deepseek-Harness\_backups` 保留 |
| 源码仓库 | `github.com/anywhere-labs/deepseek-harness-desktop`(公开,默认分支 master,标签 v2.0.2 对应已装 exe;插件包源码在其 `dsh-plugin-desktop/` 子目录) |

## 1. 阶段计划

| Phase | 内容 | 状态 |
|---|---|---|
| 0 | git 存底 + 本记录文档 | ✅ |
| 1 | 修 6 处崩溃点(每处独立构建+提交+记录) | ✅ 6/6 |
| 2 | clone 源码进 vendor ✅ → yarn 安装/构建/首次打包 ⏳ | ⏳ |
| 3 | 装配 desktop profile(4 批)+ 验证清单 | ⏳ |
| 4 | 收敛单实例 + 72h 监控 + 文档收尾 + E 盘清理 | ⏳ |

## 2. 崩溃点修复记录(Phase 1)

| # | 插件 | 位置(实际行号) | 修复内容 | 状态 |
|---|---|---|---|---|
| 1 | dsh-remote-workspace | `src/index.ts` run() :272-274 / handler :733-740 | 三流补 error 监听;`await handle` 包 try/catch(lib 原已修,src 同步) | ✅ `f11fd93` |
| 2 | dsh-session-watchdog | `src/index.ts` :163(lib :138) | `Promise.resolve(...).catch(log)` 防未处理 rejection | ✅ `891d53d` |
| 3 | dsh-context-lifecycle | `src/index.ts` :362-373(lib :346) | 回调整体 try/catch;`runCompact().catch()` | ✅ `ba8e98d` |
| 4 | dsh-stuck-loop-guard | `src/index.ts` :344(lib :269) | `await next()` 挂 catch;WeakMap key 判对象 | ✅ `6019453` |
| 5 | dsh-vision-engine | `lib/client.js` :678/:744-749 | 轮询与全局监听改为幂等注册+清理(防热重载累积) | ✅ `b4b344e` |
| 6 | dsh-project-brief | `src/index.ts` :37/:42(lib :33/:44) | fileName 路径校验 + writeFileSync try/catch | ✅ `483667d` |

> 备注:① 本机无 DSH_CHECKOUT,插件无法全量 tsc 构建(仅服务端可编译,client 缺 react 类型),遵循"lib 为准(src 编译产物,运行时加载)、src 同步"策略,已 `node --check` 全部通过;② session-watchdog/project-brief/stuck-loop-guard 的 `lib/` 被各自 .gitignore 忽略(不入库,磁盘已生效),context-lifecycle 与 vision-engine 的 lib 入库随提交;③ 发现 remote-workspace 的 lib 此前已被修复但 src 漂移,本次同步。

## 3. 防崩溃机制(救生索,已核实)

1. `dsh --profile desktop --patch <文件>` —— CLI 叠加 patch 层,可禁用任意 bundle;
2. 直接编辑 `~/.dsh/profiles/desktop/cordis.patch.yml` 加 `disabled: true`;
3. 备份恢复:`D:\Deepseek-Harness\_backups\20260822-19xxxx\`(desktop + web 六件套);
4. 每插件独立 git commit,可 `git revert`;
5. **modlens 绝不热重载**(adapter 只启动时注册,热重载会卡死服务)。

## 4. 执行日志

| 时间 | 动作 | 结果 |
|---|---|---|
| 2026-08-22 | 备份 6 个 profile 文件到 `_backups\` | ✅ |
| 2026-08-22 | 核实:源码仓库公开、标签 v2.0.2、新壳 `openBrowser:false` | ✅ |
| 2026-08-22 | git 存底 `52fe6b0` + 建立本文档 | ✅ |
| 2026-08-22 | Phase 1 完成:6 处崩溃点 src/lib 同步修复,`node --check` 全过,6 个独立提交 | ✅ |
| 2026-08-22 | Phase 2:clone 官方源码 ✅ `vendor\deepseek-harness-desktop` @ v2.0.2(commit 9d18856,浅克隆);发现官方自带打包脚本 `scripts/package-win.ts`/`package-dir.mjs`,无需反推 electron-builder | ✅ |
| 2026-08-22 | Phase 2 工具链确认:node v24.14.0 ✓ / corepack ✓ / yarn@4.18.0(仓库锁定,corepack 提供)/ 代理 127.0.0.1:7897 存活 / submodule(deepseek-ai/deepseek-harness)待 init | ✅ |

## 5. 验证清单(Phase 3 用)

- [ ] 插件中心出现全部新插件(模型选择/技能管理/文件浏览器/皮肤)
- [ ] 新会话对话可用,模型路由含 modlens 渠道
- [ ] `mcp_search`/`mcp_call` 工具出现
- [ ] 托盘、通知、更新检查、多 profile 正常
- [ ] 远程工作区(docker/ssh 缺失场景不崩)
- [ ] 无 CPU 空转;日志/Crashpad 干净
- [ ] 连续 72h 无进程退出
