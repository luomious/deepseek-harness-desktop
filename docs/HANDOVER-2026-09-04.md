# DSH 桌面项目 · 2026-09-04 全面审计归档

> 审计日期：2026-09-04
> 范围：进度/功能清单/遗漏项/安全性/代码质量，配套规范化整理 + 入库归档。
> 关联提交：本次多 commit 分组已 push（见 git log）；登记 `docs/CHANGELOG.md` 顶部条目。
> 目的：供后续会话接手本项目时的**当前状态单一事实源**（与 `plugins/INVENTORY.md`、`AGENTS.md` 配合）。

---

## 1. 项目健康结论（一句话）

**无真实密钥入库、无命令注入、无对外监听；代码语法全绿、已有测试全 PASS；本审计为纯整理 + 入库，无功能行为改动。**

## 2. 已确认现状

| 维度 | 结论 |
|---|---|
| 仓库 | `master` HEAD 090e86b（09-02）→ 本审计后 push 至最新，origin 同步 |
| 插件 | `plugins/` 28 个 + 根级 3 + profile 市场安装 3（context / better-sidebar-office / memory）= 34 组件 |
| Profile | 运行态与模板：42 deps / 35 bundles |
| 补丁体系 | `verify-patches.ps1` 29 项 ALL PASS 载体；port-user-patches 登记 |
| 测试 | startup-verify 6/6、command-guard risk 12/12、failover 单测+集成、tier-router classify 直跑 ALL PASS |
| 行尾 | 新增 `.gitattributes` 统一 LF，消除跨平台抖动 |

## 3. 未完成 / 待办（承接上层 HANDOVER 2026-08-31 的剩余计划）

| 项 | 状态 | 说明 |
|---|---|---|
| 阶段2 #3 记忆系统 | 待官方 stable（可用 memory 插件 v0.3.0 已装配验证） | P2 |
| 阶段3 #16 Mermaid | 暂缓（LaTeX 已内置，Mermaid 无 slot 注入点） | P2 |
| 阶段4 #9/#10/3c/v2.0.4 | 待官方 | P3 |
| 阶段5 Mention / 主题 | Mention 待验证输入框 slot；主题搁置 | P3 |
| 阶段6 告警/看板 UI | 巡检脚本已就位，告警/看板需装配 | 可选 |
| vendor injector `src/index.ts` 缺 H2 CSRF | lib 已修、src 未统一 | 迁移项，重建 vendor 时处理 |
| 无单测插件约 15 个 | 可按需补纯逻辑测试 | 可选 |
| 插件重复代码收敛 | 5+ 插件重写 log/resolveConfig/readJson/spawn，host-services 已有单一事实源 | 后续重构项 |
| `remote_bash` 命令白名单 | 设计保持（已做 target 校验），可加危险命令告警 | 可选 |
| `plugins/dsh-routing-suite/` 子模块失真 | PROVENANCE 记录上游需重钉 `c03de54`（含 H1-H4）；依赖上游配合 | 待上游 |

## 4. 本次改动清单（对照 CHANGELOG 顶部条目）

- **清理**：`CHANGELOG.md.fixed-node`、13 处 `.tmpdir`、6 个日志（全走回收站）。
- **加固**：`.gitignore`（`*.log`/`*.pem`/`*.key`/`.env`）+ super-injector tgz 解忽略入库 + `.gitattributes` + `repository.url` 修正 + analysis 移入 `docs/research/`。
- **文档**：INVENTORY 补 failover/memory；PROJECT_README 数量同步；HANDOVER tool-visibility 对账注记；AGENTS structure 刷新；CHANGELOG 顶部条目 + 格式修复。
- **未做（保持最小改动，避免引入风险）**：未改任何运行中代码/补丁/装配；未撤换 profile；未动 vendor/lib。

## 5. 关键验证证据

- `node --check`：plugins/lib/*.js + scripts/*.mjs 全部通过。
- 测试直跑：`node plugins/dsh-model-provider-failover/test/failover.test.mjs` → ALL TESTS PASSED；`integration.test.mjs` → ALL INTEGRATION TESTS PASSED；`classify.test.mjs` → ALL TESTS PASSED。
- `git add -n` 核验 super-injector tgz 为**单文件入库**，无 gitlink/embedded-repo 污染。
- `.tmpdir` 残留枚举并列清单回收站删除，安全守则满足。

## 6. 备份 / 回滚

- 所有删除操作走回收站（可还原）。
- 文档/配置改动均可 `git revert` 或从远端恢复。
- 无运行路径文件改动（dist/node_modules/plugins 装配未动），**无需重启桌面应用**。