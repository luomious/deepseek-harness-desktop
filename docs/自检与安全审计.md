# 自检与安全审计报告

> 日期:2026-08-22 | 范围:D:\Deepseek-Harness 插件(src/lib)+ 自建脚本 + vendor 依赖
> 状态:审计完成,发现 6 处问题已全部修复(5 处代码 + 1 处依赖),1 项待生效说明

## 一、安全问题(已修复)

| # | 问题 | 危害 | 修复 |
|---|---|---|---|
| 1 | **file-explorer `trusted()` Origin 端口硬编码 3080** | 桌面版(43120)下文件浏览器 API 全部 403(功能故障) | 改同源校验:Origin 端口须等于 Host 端口,兼容任意端口 `9f62cdb` |
| 2 | **skills-manager `trusted()` 同款 3080 硬编码** | 桌面版下技能管理 API 全部 403 | 同上 `0e451dd` |
| 3 | **vision-engine 7 条 HTTP 路由无本机校验** | 本地任意进程可调 config/diag 等(GET 路由,信息暴露) | 补回环+Host 本机校验(注意 GET 同源无 Origin,不做 Origin 要求) `6695363` |
| 4 | **context-lifecycle `/context-lifecycle` 路由无校验** | 同上(状态/decide 接口) | 补回环+Host 校验(src+lib) `9f01c58` |
| 5 | **modlens-guard `void hotReapply(ctx)` 未 catch** | 未处理 rejection 风险(进程级崩溃隐患) | 补 `.catch(log)`(2 处) `a0161ad` |
| 6 | **内置 pnpm 11.7.0 高危漏洞** GHSA-qrv3-253h-g69c(路径穿越可越界建符号链接) | 安装恶意 lockfile 时可越界写 | 升级 11.21.0(vendor/ 被 gitignore,变更在本地工作区 + node_modules 已更新;`下次打包生效`) `d3f3290`(脚本入库) |

## 二、审计通过项(无需修复)

| 项 | 结论 |
|---|---|
| 命令注入 | 全部使用 `spawn(array)` 无 shell 拼接;skills-manager 的 `mkdir -p` 用 q() 引号转义 + 技能名 kebab-case 强校验 |
| 路径穿越 | file-explorer(isPathAllowed realpath 主目录包含+符号链接防逃逸)、skills-manager(删除路径拒 `..`)、project-brief(已修 fileName)、vision-engine paste-img(PASTE_ROOT 包含+图片扩展名白名单) |
| SSRF | web-fetch-local:拒绝 localhost/.local/.internal/私网 IP,全量 DNS 记录复查,重定向逐跳复查 |
| HTTP 体量 | file-explorer/skills-manager/remote-workspace 均有 64KB 请求体上限;file-explorer 读文件 2MB 上限、单目录 500 项上限 |
| eval/new Function | 仅 super-injector 编译本地插件源码(设计如此,来源为本地受信任文件) |
| 依赖漏洞 | web profile `pnpm audit --prod`:**0 已知漏洞**;vendor `yarn npm audit`:**1 个高危(pnpm,已修)** |
| 崩溃点 | 此前 6 处(remote-workspace/watchdog/context-lifecycle/stuck-loop-guard/vision-engine/project-brief)已修并验证 |

## 三、生效说明(重要)

1. **插件修复(file-explorer/skills-manager/vision-engine/context-lifecycle/modlens-guard)**:
   已写入仓库(运行时加载的 lib 均已改)。**重启自建 exe 后生效**;
2. **内置 pnpm 升级**:已更新 vendor 源码与 node_modules,**重新打包 exe(`scripts/package-vendor.ps1`)后生效**(当前 exe 仍内置 11.7.0,不阻塞日常使用;建议下次需要重建时顺带升级);
3. 生效后可自测:桌面版打开文件浏览器/技能管理,应不再 403;`/vision-engine/config` 带 Host 校验正常返回。

## 四、方法回顾

本次修复遵循既有机制:每项独立提交可回滚;lib 为准(src 同步);`node --check` 语法校验全过;未触碰运行中实例(生效靠重启)。
