# Electron 运行时二进制（不提交到 git，通过 Release 发布）
app/
*.exe
*.dll
*.pak
*.dat
*.bin
locales/

# 临时文件
*.zip
*.7z
extracted/
node_modules/
dist/

# OS
Thumbs.db
.DS_Store

test-plugin/

# 工作区记忆（本地辅助数据，不入库）
.workbuddy/

# pnpm store（本地依赖缓存，不入库）
.pnpm-store/

# 本机开发辅助脚本（含个人路径，不入库）
approve-builds.ps1
install-dsh-plugins.ps1

# 临时打包/验证脚本目录（不入库）
tools/

# DSH web profile 本地备份（回滚用，不入库）
.dsh-web-profile-*/

# dsh-safe-delete 回收区（运行时生成，不入库）
.dsh-trash/

# dsh 运行时数据（如 dsh-tool-search.json，不入库）
.dsh/

# 本地文档/笔记（不入库）
docs/
