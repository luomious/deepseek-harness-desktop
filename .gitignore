# Electron 运行时二进制（不提交到 git，通过 Release 发布）
app/
*.exe
*.dll
*.pak
*.dat
*.bin
locales/

# 临时文件
*.zip
*.7z
extracted/
node_modules/
dist/

# OS
Thumbs.db
.DS_Store

test-plugin/

# 工作区记忆（本地辅助数据，不入库）
.workbuddy/

# pnpm store（本地依赖缓存，不入库）
.pnpm-store/

# 本机开发辅助脚本（含个人路径，不入库）
approve-builds.ps1
install-dsh-plugins.ps1

# 临时打包/验证脚本目录（不入库）
tools/

# DSH web profile 本地备份（回滚用，不入库）
.dsh-web-profile-*/

# dsh-safe-delete 回收区（运行时生成，不入库）
.dsh-trash/

# dsh 运行时数据（如 dsh-tool-search.json，不入库）
.dsh/

# 本地文档/笔记（不入库）
docs/

# 本地分析/排查临时目录（如 transcribe 等，不入库）
.dsh-analysis/

# 外部仓库 clone（独立 repo，按各自 README 单独拉取，不入库）
plugins/dsh-routing-suite/

# 插件构建产物（tsdown/tsc 输出，不入库；源码在 src/）
plugins/*/lib/**/*.d.ts
plugins/*/lib/**/*.map

# 插件本地测试脚本（开发自测用，可选保留）
# plugins/*/test-*.mjs

# agent-presets 本地副本（运行时用 ~/.dsh/.agent-presets，不入库）
agent-presets/

# 涓撳畧/鐢熷懡鍛ㄦ湡鎻掍欢杩愯鏃舵暟鎹?涓嶅叆搴擄級
dsh-stuck-loop-guard/data/
dsh-context-lifecycle/data/

# 鎶ゅ崼鎻掍欢鏋勫缓浜х墿锛堟簮鐮佸湪 src/锛?map/.d.ts 涓嶅叆搴擄級
dsh-stuck-loop-guard/lib/**/*.map
dsh-stuck-loop-guard/lib/**/*.d.ts
dsh-context-lifecycle/lib/**/*.map
dsh-context-lifecycle/lib/**/*.d.ts
# 守护/生命周期插件运行数据（不入库）
dsh-stuck-loop-guard/data/
dsh-context-lifecycle/data/

# 守护插件构建产物（源码在 src/，.map/.d.ts 不入库）
dsh-stuck-loop-guard/lib/**/*.map
dsh-stuck-loop-guard/lib/**/*.d.ts
dsh-context-lifecycle/lib/**/*.map
dsh-context-lifecycle/lib/**/*.d.ts
